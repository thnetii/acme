<?xml version="1.0" encoding="utf-8" ?>
<Project>
  <PropertyGroup Label="Common Package Metadata">
    <Authors Condition="'$(Authors)'==''">Fredrik Høisæther Rasch</Authors>
    <Product Condition="'$(Product)'==''">TH-NETII Automatic Certificate Management Environment (ACME)</Product>
    <Company Condition="'$(Company)'==''">TH-NETII Rasch Solutions</Company>
    <Copyright Condition="'$(Copyright)'==''">Written by Fredrik Høisæther Rasch, $([System.DateTime]::Now.Year)</Copyright>
  </PropertyGroup>
  <PropertyGroup Label="Repository Metadata">
    <RepositoryType Condition="'$(RepositoryType)'==''">git</RepositoryType>
    <RepositoryUrl Condition="'$(RepositoryUrl)'==''">https://github.com/thnetii/acme.git</RepositoryUrl>
  </PropertyGroup>
  <PropertyGroup>
    <PackageLicenseUrl>https://github.com/thnetii/acme/raw/master/LICENSE</PackageLicenseUrl>
    <PackageProjectUrl>https://github.com/thnetii/acme</PackageProjectUrl>
  </PropertyGroup>
  <!--
  <ItemGroup>
    <Compile Remove="obj\**\*.*" />
    <Compile Remove="bin\**\*.*" />
    <None Remove="obj\**\*.*" />
    <None Remove="bin\**\*.*" />
  </ItemGroup>
  -->
</Project>
